For all the challenges, use namespaces.

XML Parser
==========
* create the code that will parse the contents of sample.xml
* the script should output the unique list of tags used in the xml file
* the script should output all text content as a flat string
* implement the same requirements using the same method

SimpleXML
=========
* instantiate SimpleXML using a string
* convert the entire XML to string and echo contents
* instantiate SimpleXML using a file
* add a new movie to the xml (Batman)
* add a new attribute (imdb)

SimpleXML and DomDocument
=========================
* import the DomDocument object into SimpleXML
* import the SimpleXML object into DomDocument

DOM Document
============

